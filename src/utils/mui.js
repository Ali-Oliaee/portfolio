import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#fca311',
    },
    secondary: {
      main: '#e5e5e5',
    },
    info: {
      main: '#e5e5e5',
    },
  },
})

export default theme
